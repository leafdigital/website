import { Header } from "leaf-website";

/**
 * The whole bar, exactly as it ships — logo, nav, and the Free scan CTA. It
 * takes no props, so there is one honest story. The bar is full-width by
 * design, hence the single-story card mode in the config.
 */
export const Default = () => <Header />;
