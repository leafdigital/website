import { Footer } from "leaf-website";

/**
 * The whole bar as it ships: copyright plus Privacy / Support / Contact. Takes
 * no props. Full-width by design, like Header.
 */
export const Default = () => <Footer />;
