import { Footer } from "leaf-website";

/**
 * The whole bar as it ships: dark `surface-deep` ground, 55%-white copyright,
 * Privacy / Support / Contact and the locale switcher. Takes no props, and is
 * full-bleed by design. The year is live — the capture harness pins its clock,
 * so a screenshot may show an older one.
 */
export const Default = () => <Footer />;
