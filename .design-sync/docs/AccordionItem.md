---
category: Primitives
---

# AccordionItem

One row of an Accordion. Needs a unique `value`; gets a divider from its siblings and a muted background when open.

Part of [Accordion](./Accordion.md) — only renders correctly inside
`<Accordion><AccordionItem>`, which supplies its open/closed context.
