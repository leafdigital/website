---
category: Primitives
---

# CardTitle

Title line inside CardHeader. Renders a div — use a real heading element when the title belongs in the page outline.

Part of [Card](./Card.md) — compose it inside `<Card>`, never on its own.
